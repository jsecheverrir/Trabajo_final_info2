#ifndef OBJETO_H
#define OBJETO_H

class Objeto {
private:
         // Atributos comunes para todos los objetos
         // Por ejemplo, posición, etc.

public:
    // Métodos comunes para todos los objetos
    Objeto(); // Constructor
    virtual ~Objeto(); // Destructor virtual
    // Otros métodos y atributos según tus necesidades
};

#endif // OBJETO_H
